# Docker勉強会補足資料

## 環境

dockerが動作する環境前提です。構築手順については割愛します。

docker動作確認コマンド

```bash
docker run ubuntu echo hello world
```

コンソールに `hello world` と出力されればOKです。

## リポジトリ説明

各ディレクトリにあるcmd.txtに使用するコマンドが羅列してあります。
必要に応じてコピペして使ってください。  
